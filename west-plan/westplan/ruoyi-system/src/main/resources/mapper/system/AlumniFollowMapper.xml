<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.AlumniFollowMapper">
    
    <resultMap type="AlumniFollow" id="AlumniFollowResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="followUserId"    column="follow_user_id"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <sql id="selectAlumniFollowVo">
        select id, user_id, follow_user_id, create_time from alumni_follow
    </sql>

    <select id="selectAlumniFollowList" parameterType="AlumniFollow" resultMap="AlumniFollowResult">
        <include refid="selectAlumniFollowVo"/>
        <where>  
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="followUserId != null "> and follow_user_id = #{followUserId}</if>
        </where>
    </select>
    
    <select id="selectAlumniFollowById" parameterType="Long" resultMap="AlumniFollowResult">
        <include refid="selectAlumniFollowVo"/>
        where id = #{id}
    </select>

    <insert id="insertAlumniFollow" parameterType="AlumniFollow" useGeneratedKeys="true" keyProperty="id">
        insert into alumni_follow
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="followUserId != null">follow_user_id,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="followUserId != null">#{followUserId},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateAlumniFollow" parameterType="AlumniFollow">
        update alumni_follow
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="followUserId != null">follow_user_id = #{followUserId},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteAlumniFollowById" parameterType="Long">
        delete from alumni_follow where id = #{id}
    </delete>

    <delete id="deleteAlumniFollowByIds" parameterType="String">
        delete from alumni_follow where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>