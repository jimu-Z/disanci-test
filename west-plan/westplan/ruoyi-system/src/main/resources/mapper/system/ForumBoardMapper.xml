<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ForumBoardMapper">
    
    <resultMap type="ForumBoard" id="ForumBoardResult">
        <result property="id"    column="id"    />
        <result property="boardName"    column="board_name"    />
        <result property="boardDesc"    column="board_desc"    />
        <result property="postCount"    column="post_count"    />
        <result property="sort"    column="sort"    />
        <result property="status"    column="status"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectForumBoardVo">
        select id, board_name, board_desc, sort, status, create_time, update_time from forum_board
    </sql>

    <!-- 基础CRUD（MyBatis Generator生成） -->
    <select id="selectByPrimaryKey" resultMap="ForumBoardResult" parameterType="java.lang.Long">
        <include refid="selectForumBoardVo"/>
        from forum_board
        where id = #{id,jdbcType=BIGINT}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from forum_board
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insert" parameterType="com.ruoyi.system.domain.ForumBoard">
        insert into forum_board (id, board_name, board_desc, sort, status,
                                 create_by, create_time, update_by, update_time, remark)
        values (#{id,jdbcType=BIGINT}, #{boardName,jdbcType=VARCHAR}, #{boardDesc,jdbcType=VARCHAR},
                #{sort,jdbcType=INTEGER}, #{status,jdbcType=INTEGER}, #{createBy,jdbcType=VARCHAR},
                #{createTime,jdbcType=TIMESTAMP}, #{updateBy,jdbcType=VARCHAR}, #{updateTime,jdbcType=TIMESTAMP},
                #{remark,jdbcType=VARCHAR})
    </insert>

    <insert id="insertSelective" parameterType="com.ruoyi.system.domain.ForumBoard">
        insert into forum_board
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="boardName != null">board_name,</if>
            <if test="boardDesc != null">board_desc,</if>
            <if test="sort != null">sort,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id,jdbcType=BIGINT},</if>
            <if test="boardName != null">#{boardName,jdbcType=VARCHAR},</if>
            <if test="boardDesc != null">#{boardDesc,jdbcType=VARCHAR},</if>
            <if test="sort != null">#{sort,jdbcType=INTEGER},</if>
            <if test="status != null">#{status,jdbcType=INTEGER},</if>
            <if test="createBy != null">#{createBy,jdbcType=VARCHAR},</if>
            <if test="createTime != null">#{createTime,jdbcType=TIMESTAMP},</if>
            <if test="updateBy != null">#{updateBy,jdbcType=VARCHAR},</if>
            <if test="updateTime != null">#{updateTime,jdbcType=TIMESTAMP},</if>
            <if test="remark != null">#{remark,jdbcType=VARCHAR},</if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.ruoyi.system.domain.ForumBoard">
        update forum_board
        <set>
            <if test="boardName != null">board_name = #{boardName,jdbcType=VARCHAR},</if>
            <if test="boardDesc != null">board_desc = #{boardDesc,jdbcType=VARCHAR},</if>
            <if test="sort != null">sort = #{sort,jdbcType=INTEGER},</if>
            <if test="status != null">status = #{status,jdbcType=INTEGER},</if>
            <if test="updateBy != null">update_by = #{updateBy,jdbcType=VARCHAR},</if>
            <if test="updateTime != null">update_time = #{updateTime,jdbcType=TIMESTAMP},</if>
            <if test="remark != null">remark = #{remark,jdbcType=VARCHAR},</if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.ruoyi.system.domain.ForumBoard">
        update forum_board
        set board_name = #{boardName,jdbcType=VARCHAR},
            board_desc = #{boardDesc,jdbcType=VARCHAR},
            sort = #{sort,jdbcType=INTEGER},
            status = #{status,jdbcType=INTEGER},
            create_by = #{createBy,jdbcType=VARCHAR},
            create_time = #{createTime,jdbcType=TIMESTAMP},
            update_by = #{updateBy,jdbcType=VARCHAR},
            update_time = #{updateTime,jdbcType=TIMESTAMP},
            remark = #{remark,jdbcType=VARCHAR}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 自定义方法SQL -->
    <select id="selectForumBoardList" resultMap="ForumBoardResult" parameterType="com.ruoyi.system.domain.ForumBoard">
        <include refid="selectForumBoardVo"/>

        <where>
            <if test="boardName != null and boardName != ''">
                and board_name like concat('%', #{boardName}, '%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by sort asc, create_time desc
    </select>

    <select id="selectEnableBoardList" resultMap="ForumBoardResult">
        select
        b.id, b.board_name, b.board_desc, b.sort, b.status, b.create_time, b.update_time,
        count(p.id) as post_count
        from forum_board b
        left join forum_post p on b.id = p.board_id
        <!-- 核心修改点：在关联时就过滤掉不符合条件的帖子 -->
        and p.audit_status = 1
        and p.is_public = 1
        where b.status = 1
        group by b.id
        order by b.sort asc, b.create_time desc
    </select>

    <update id="updateBoardSort">
        <foreach collection="boards" item="board" separator=";">
            update forum_board
            set sort = #{board.sort}, update_time = now()
            where id = #{board.id}
        </foreach>
    </update>

    <select id="countByBoardName" resultType="java.lang.Integer">
        select count(1)
        from forum_board
        where board_name = #{boardName}
        <if test="excludeId != null and excludeId != 0">
            and id != #{excludeId}
        </if>
    </select>

    <select id="countPostByBoardIds" resultType="java.lang.Integer">
        select count(1)
        from forum_post
        where board_id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>